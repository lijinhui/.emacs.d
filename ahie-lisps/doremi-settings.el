;; -*- Emacs-Lisp -*-

;; Time-stamp: <2009-12-22 18:10:47 Tuesday by ahei>

(require 'doremi)
(require 'doremi-cmd)
(require 'doremi-mac)
(require 'thumb-frm)
(require 'menu-bar+)
(require 'zoom-frm)

;; (require 'setup-keys)
(require 'facemenu+)

(setq doremi-up-keys   '(?\M-p ?p ?k))
(setq doremi-down-keys '(?\M-n ?n ?j ?\ ))

(setq doremi-RGB-increment-factor 256)
